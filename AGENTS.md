# Repository instructions

This public repository stores Phantom third-party audit reports as PDF files. See `README.md` for the repository purpose and security contact.

There is no application code, package manifest, or repository-provided build, lint, or test command. Preserve report files exactly unless the task explicitly requires adding or replacing an audit report.